import { menu } from "./menu";
import { contacts } from "./users";

//-----------------------|| MENU ITEMS ||-----------------------//

const menuItems = {
  items: [menu, contacts],
};

export default menuItems;
